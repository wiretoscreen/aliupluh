version = "0.1.0"

description = "Renders LaTeX Formulas.\n* Service is provided by quicklatex.com"

aliucord {
    changelog.set(
        """
        * **0.1.0**: Initial Release
        """
            .trimIndent()
    )
    deploy.set(true)
}
